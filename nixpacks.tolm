# Fichier de configuration final pour Nixpacks

[phases.build]
# On installe CHROMIUM et ses dépendances, car il est plus stable en serveur.
cmds = ["playwright install chromium --with-deps"]

[start]
# On s'assure que le nom du fichier correspond bien à notre fichier principal.
cmd = "python bot.py"
